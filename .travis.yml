language: java

addons:
  sonarcloud:
    organization: "1809-spark-usf"
    
    

jobs:
  include:
    - stage: pre-build
      script: cd Interview_API
      script: mvn clean
    
    - stage: build
      script: mvn package
      
    - stage: quality
      script: mvn sonar:sonar
